import { FilteredResultPipe } from './filtered-result.pipe';

describe('FilteredResultPipe', () => {
  it('create an instance', () => {
    const pipe = new FilteredResultPipe();
    expect(pipe).toBeTruthy();
  });
});
